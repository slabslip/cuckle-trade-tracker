# League-scoped raw Sleeper / tape artifacts land here via:
#   LEAGUE_ID=<id> node sleeper-sync.mjs
#   node build.mjs <id>
#
# Shared player/curve caches stay under data/ (see lib.mjs SHARED_JSON).
